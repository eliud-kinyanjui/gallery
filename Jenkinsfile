pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                echo 'Clone App Repository'
            }
        }
        stage('Install Dependancies') {
            steps {
                echo 'Run npm install'
            }
        }
    }
}